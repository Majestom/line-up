# Environment Configuration for Line-Up Application
#
# SECURITY BEST PRACTICE:
# - Never commit .env files to version control
# - Copy this file to .env and add your actual values
# - Keep production secrets in secure environment variable stores
# - Use different keys for different environments (dev, staging, production)
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and replace the placeholder values
# 3. For development: use the free tier key shown below
# 4. For production: get your own key from https://app.reqres.in/signup

# =============================================================================
# API CONFIGURATION
# =============================================================================

# ReqRes API Key
# Free tier (no signup required): reqres-free-v1
# Production: Get from https://app.reqres.in/signup
REQRES_API_KEY=reqres-free-v1

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment (development, staging, production)
NODE_ENV=development

# API Base URLs (used by frontend to connect to backend)
VITE_API_BASE_URL=http://localhost:8000

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# These are used by docker-compose.yml and don't typically need changes
FRONTEND_PORT=5173
BACKEND_PORT=8000